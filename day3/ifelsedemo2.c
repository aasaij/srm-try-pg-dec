//Program to demonstrate if else
#include <stdio.h>

int main(){
	//declaration
	double score;
	//executable statements
	scanf("%lf", &score);
//	if (score > 40)
	if (score,0)
		printf("Pass");
	else
		printf("Fail");
	return 0;
}